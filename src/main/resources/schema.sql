CREATE TABLE IF NOT EXISTS employee
(
    id        INTEGER PRIMARY KEY,
    name VARCHAR(50),
    email  VARCHAR(50),
    jobTitle  VARCHAR(50),
    phone     VARCHAR(50),
    imageUrl  VARCHAR(50)

);